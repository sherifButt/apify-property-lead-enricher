{
  "title": "Property Lead Enricher Input",
  "description": "Input schema for the UK property lead enrichment Actor",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Company name, postcode (e.g. CF10), or director name to search for",
      "editor": "textfield",
      "prefill": "Cardiff"
    },
    "searchType": {
      "title": "Search Type",
      "type": "string",
      "description": "Type of search to perform",
      "editor": "select",
      "enum": ["postcode", "companyName", "directorName"],
      "default": "postcode"
    },
    "enrichSources": {
      "title": "Enrichment Sources",
      "type": "array",
      "description": "Which data sources to use for enrichment",
      "editor": "json",
      "items": {
        "type": "string",
        "enum": ["companiesHouse", "landRegistry", "propertyListings", "linkedin"]
      },
      "default": ["companiesHouse", "landRegistry"],
      "prefill": ["companiesHouse", "landRegistry"]
    },
    "maxResults": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of leads to enrich per run",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for scraping modules",
      "editor": "proxy",
      "sectionCaption": "Proxy settings",
      "sectionDescription": "Required for Rightmove, Zoopla, and LinkedIn scraping"
    }
  },
  "required": ["searchQuery", "searchType"]
}
